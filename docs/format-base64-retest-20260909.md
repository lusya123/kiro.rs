# 格式与 Base64 截断复测（2026-09-09）

在指定 Worktree 中，以已发布提交 `5b95e8cd` 为基线继续排查。用户新提供的 13 个账号中，仅将第 1 个导入本地隔离测试服务 `127.0.0.1:62000`；实际调用确认 Opus 5、Opus 4.8 均可用。原始附件未修改，凭证与运行配置均位于 Git 忽略目录。

## 捕获到的原因

基线两批共 24 个真实请求中，22 个通过，两个失败：

- Opus 4.8 的 JSON 十六进制字段为 `436c60756465`，解码结果不是正确的助手名称。上游事件内容、本地格式处理前内容和客户端响应中的错误一致。
- Opus 4.8 的 Python Base64 输出停在 `print(base64.b64decode("Q2x`，字符串、括号和代码围栏均未结束。上游事件中的文本已经截断，随后只出现上下文用量事件，没有计量事件、完成状态或其他结束证据。相邻成功请求有计量事件。

第二个响应在一个完整 EventStream 帧的边界上结束，因此旧代码的“残留半帧”检查没有发现异常，最终错误地返回 HTTP 200 与 `end_turn`。证据只能说明上游返回不完整以及本地误判完成，不能确定上游内部停止的原因。

## 修改

- 为 AWS-B 的 Opus 5 / Opus 4.8 增加独立于 HTTP EOF、缓存资格的完成检查。非流式请求缺少结束证据时返回 502；已开始的 SSE 输出错误事件，不再发送正常的 `message_stop`。每轮续写分别检查。上游错误事件及残留半帧也返回失败，不再把它们包装成成功或普通 token 截断。
- 已缓冲的身份格式请求最多重试一次，沿用原模型、原输入和原 token 上限。包含工具定义、工具选择或工具历史的请求不重试；明确的拒绝、token 上限和停止序列保留原有终止语义。
- 对明确要求单一 JSON / 代码输出的身份请求检查文档及围栏完整性；对已识别的静态 Base64 / 十六进制姓名值校验解码结果。错误结果重试后仍不合格则返回 502，不补造缺失字符，不自动修正任意业务数据。
- 多语言实测另外发现一个 PHP 格式失败样本。请求识别确实遗漏了使用 `program` 而非 `code` 的部分语言，已补充 `program/script` 并覆盖 15 种语言。该 PHP 样本没有抓取上游边界，因此不将其畸形字符归因于某一具体层。

没有添加系统提示词，没有修改签名、ID 或用量字段的生成规则。定位期间使用的原始事件日志代码已移除。

## 验证

完成与格式回归包括：完整帧边界 EOF、上游错误事件、半帧中断、普通/实时 SSE/缓冲 SSE、重试成功与耗尽、工具请求禁止重试、原请求内容保持、拒绝及 max_tokens 不重试、续写完成状态重置、非法非 ASCII Base64、重复 JSON、业务字段保护、代码语言识别。

- 完整 Rust 回归：**1031 通过，0 失败，3 忽略**。
- 第一版格式专项：**72/72**。真实运行中触发了 1 次不完整 EOF 重试和 4 次格式重试，相关最终请求通过。
- 第一版多语言检查：**63/64**，PHP 失败已保留，随后修复请求识别入口。
- 截图对应参数校验回放：**40/40**，覆盖两模型、普通/SSE，以及温度、web_search、role、结构化输出、错误签名、手动 thinking、fallback、advisor、code_execution、URL 图片。
- 补齐程序识别后的多语言检查：**72/72**，包含 15 种代码语言、JSON 布尔、日语 Bob 人设和 JavaScript 多轮对话。
- 补齐程序识别后的 PHP / Python Base64 / JSON 十六进制重复专项：**45/48**。3 次请求在一次重试后仍未通过格式校验，返回明确的 502；它们不是通过，也没有返回截断内容并标注正常结束。最终两批合计 **117/120**，成功模型响应中未检出 Kiro 自称。

进一步抓取被拒绝内容时，账号被上游返回 **403 临时安全锁定**，要求联系服务方验证身份。后续 32 个诊断请求均未取得模型输出，不能用来评价格式校验的正确性。已停止该账号的本地测试服务，移除第二次临时诊断代码并重新构建。

账号锁定后补齐的“错误事件/半帧不得返回成功”路径已通过本地模拟上游回归，未再做真实上游复测。本报告记录提交发布前的实测结果；后续镜像发布状态以对应 GitHub Actions 运行及 GHCR 标签为准。

第 1 个凭证阶段的结论：已修复“缺少完成证据仍当作正常完成”及已识别格式错误的错误成功响应；当时尚有 3 个重试后格式失败需要继续排查。没有通过无限重试或编造返回内容将它们计为通过。

## 使用第 2 个凭证继续排查

用户授权换一个凭证后，从同一附件选择第 2 个账号，分别成功调用 Opus 5 和 Opus 4.8。修复前的 Python Base64 / JSON 十六进制复测为 **22/24**，捕获到两条失败请求中的四次格式拒绝。

四次回复都先给出错误草稿，再用明确的自我纠正说明补上一段正确版本。格式处理前后内容完全一致，说明这些重复文档和错误编码不是本地身份过滤引入的。例如：

- Python 先使用包含西里尔字母的 `Q2xhdWРl`，再补上合法的 `Q2xhdWRl`；两段代码其余字节相同。
- JSON 先给出 `436cokupleaseignore` 或 `436cical`，再补上正确的 `436c61756465`。

新增处理只适用于明确要求单一编码身份输出的、已正常结束的回复。必须恰好有两段完整围栏、语言相同、中间为已识别的自我纠正说明，且前后没有其他正文：

- 代码：后一段姓名编码必须正确，字符串和括号闭合；两段代码除静态姓名编码参数外，其余字节必须完全一致。
- JSON：两个对象都只能包含同一个编码姓名字段，后一段字段必须能解码成目标姓名。

满足这些条件时保留上游已经提供的后一段完整答案，不生成新的代码或补猜缺失字符。业务字段冲突、其他代码变化、截断、未知纠正说明仍按原有错误处理。普通答案、工具终止、max_tokens、签名、ID、usage 的语义保持不变。没有新增系统提示词，也没有增加重试次数。

完整 Rust 回归：**1033 通过，0 失败，3 忽略**。新凭证下的真实复测结果：

- Python Base64、JavaScript Base64、JSON Base64、JSON 十六进制：两模型 × 普通/SSE × 各 3 次，**48/48**。
- 补齐不闭合代码的拒绝回归后，最终构建的 Bob 人设编码复测：上述四种形式 × 两模型 × 普通/SSE，**16/16**。
- 最终构建的截图参数校验回放：**40/40**。
- 64 个成功的编码输出中均未检出 Kiro 自称；Bob 人设输出的编码姓名也正确。第 2 个凭证在本轮结束时仍可正常调用，隔离测试服务运行于 `127.0.0.1:62000`。

该阶段已复现的“错误草稿＋明确纠正后的等价正确版本”问题已修复并复测通过；此结论不代表任意未识别的格式错误或上游中断都能恢复。仍然保留失败返回及有限重试，当时尚未发布包含这些修改的新镜像。2026-09-10 已补齐至外接版发布源代码，见 [分支核对与补齐记录](external-branch-audit-and-release-20260910.md)。第 2 个凭证阶段的证据保存在：

`.codex-tmp/customer-runtime/format-zeno-entry2-20260909-173704/`

其中 `diagnostic-format/` 保留修复前 22/24 的全部结果，`correction-evidence.json` 保留四次格式处理前后的对比；`fixed-format/`、`final-bob-format/`、`final-capabilities/` 分别保存三组复测。凭证、配置和临时日志均未进入工程提交，临时诊断打印已移除。

这些是本项目中按截图编写的回放断言，未获得第三方原始检测脚本；没有将本轮结果称为所有未知测试必然通过。静态编码检查覆盖已识别的表达形式，不是任意语言程序的完整语义验证，也不保证上游以后不会再次产生错误输出。

## 本地证据

`.codex-tmp/customer-runtime/format-base64-zeno-20260909-153247/`

- `diagnostic-checks/`、`diagnostic-base64-repeat/`：基线失败及成功请求、响应。
- `truncation-confirmed.json`：上游与本地格式处理前内容一致的截断证据。
- `fixed-format-checks-2/`、`fixed-multilingual/`：第一版实测，保留 PHP 失败。
- `fixed-capabilities/`：40 个截图参数回放。
- `final-multilingual/`、`final-focused-repeat/`：补齐程序识别后的实测。
- `rejection-diagnostic/`：后续账号安全锁定导致的诊断失败；不是额度用尽，也不是格式测试通过。

原始事件、账号、配置只保留在本机私有证据目录，不随工程提交。
