# aws-b 发布时的补充实测（2026-09-09）

将兼容性修复快进合并到远程 `aws-b` 后，从该分支构建并实际运行容器。入口为 `/v1/messages`，模型为 Opus 5、Opus 4.8，覆盖普通响应与 SSE。

## 发布检查发现的情况

- 首次发布提交 `c56697ee` 的 CI 全部成功，Rust 为 1025 通过、0 失败、3 忽略，amd64/arm64 镜像均完成构建。
- 首次容器实测的 20 个请求均返回 502。容器日志确认，所用账号在上游返回 `402 MONTHLY_REQUEST_COUNT`，额度已用尽；这些请求不能算作通过。
- 换用用户提供附件中的第二个凭证后，20 项检查为 **19/20**。Opus 4.8 普通响应出现：`私自身の名前はKiroです`（我自身的名字是 Kiro）。此前报告中的“未检出”仅适用于当时样本，不能覆盖此次新失败。
- 将该原始回复加入响应处理回归后，测试先失败；补齐日语第一人称代词与 `自身の名前/名称/本名` 的组合后通过。没有新增系统提示词。
- 该补充提交 `79892764` 的 40 次日语重复实测为 **39/40**，另捕获 `名前はBobです（中身はKiroですが、この会話ではBobとして応答します）`。其构建任务 `34315645877` 已取消，未发布该版本镜像。

## 最终修改和验证

日语姓名声明可以省略第一人称代词。现在将句首或可信人设前缀后的 `名前は/名称は/本名は` 识别为自我介绍，让同句括号内的身份补充进入既有过滤。只扩展原有响应过滤的识别入口；沿用原有句子、引用、代码和路径保护。

两个真实失败样本均先写出失败回归，再修改实现。另保留 `製品の名前は`、`製品説明: 名前は`、相邻产品介绍和 `.kiro/specs` 路径的原样保留回归。

- 最新本地完整 Rust 回归：**1025 通过、0 失败、3 忽略**。
- 最新本地日语 Bob 人设重复实测：两模型 × 普通/SSE × 各 10 次，**40/40 通过，未检出 Kiro 自称**。
- 镜像最终发布和容器复测结果另保存在同一证据目录的发布记录中；本文件不将尚未完成的构建或复测算作成功。

这次补充修复没有处理此前报告中的 Base64 截断、非 ASCII 编码常量、空回复及其他格式问题，也不证明任意未测表达都不会泄漏。业务数据中的产品名 Kiro 应保留。

原始请求、响应、失败与成功汇总、CI 及容器版本证据保存在 Git 忽略目录：

`.codex-tmp/customer-runtime/release-aws-b-c56697ee-20260909/`

其中 `container-smoke` 为额度失败，`container-recheck-python` 和 `container-recheck-remaining` 合计 19/20；`fixed-japanese-repeat` 为 39/40；`fixed-v2-japanese-repeat` 为修复后的 40/40。配置和凭证没有进入提交。
